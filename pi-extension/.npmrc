# Minimum release age: don't install packages published less than 7 days ago.
# Mitigates supply-chain attacks from compromised/malicious new releases.

# npm (days)
min-release-age=7
ignore-scripts=true
